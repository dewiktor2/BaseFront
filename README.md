# BaseFront

Angular 22 application using NGXS and NG-ZORRO.

## Development

Run `npm install` and then `npm run start`. The application is served at `http://localhost:4200/`.

## Build

Run `npm run build` to create a production build in `dist/`.
